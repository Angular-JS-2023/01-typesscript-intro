
// Manejar const
const name: string = 'Strider';
// name = '123';

// Puede ser un número o cadena de texto
let hpPoints: number | string = 95;
hpPoints = 'FULL';

const isAlive: boolean = true;

console.log({ name, hpPoints, isAlive });

export {};
